# Monolith on Render: same host serves UI + /api (preferred — no CORS).
# Leave unset to auto-use window.location.origin + /api in production builds.
#
# Separate frontend host (static site on another URL): uncomment and set full API host:
# VITE_API_URL=https://ewc-erp.onrender.com
